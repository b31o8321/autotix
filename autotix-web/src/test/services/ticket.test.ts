// TODO: Service test — mock umi-request and verify URL/params.
import { describe, it } from 'vitest';

describe('ticket service', () => {
  it('listTickets hits /api/desk/tickets with filter params', () => {
    // TODO: spy on request module; call listTickets({status:'OPEN',limit:10}); assert
  });
});
