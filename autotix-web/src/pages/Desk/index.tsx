// TODO: Desk (ticket list) page.
//   - Filter bar: status, channel, assignee, search box
//   - Two-pane layout: list (left) + selected ticket detail (right, optional)
//   - Pagination
import { Card } from 'antd';

export default function DeskPage() {
  // TODO: useState for filters; useEffect to call listTickets; Table columns
  return <Card title="Desk">{/* TODO: ticket list table */}</Card>;
}
