// TODO: Login page.
//   - antd Form: email + password
//   - on submit: login() -> store tokens (localStorage or in-memory + httpOnly cookie?)
//   - redirect to /desk on success
//   - show error toast on 401
import { Card } from 'antd';

export default function LoginPage() {
  return <Card title="Login">{/* TODO: antd Form */}</Card>;
}
